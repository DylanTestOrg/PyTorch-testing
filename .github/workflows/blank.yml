name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  pull_request_target:

  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2

      - name: Run a one-line script
        run: cat README.md
        
      - run: echo ${{ github.ref }}
      
      - run: printenv
      
      - run: echo ${{ secrets.MY_SECRET }}
